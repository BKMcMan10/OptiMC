#include "core/Model.h"

void Model::AddBox(Minecraft::TextureType type, static const Minecraft::TextureFormat* texture, glm::vec3 size, glm::vec3 position, glm::vec2 textureSize, glm::vec2 texturePosition, glm::vec3 GlobalPosition)
{
	//Minecraft::Renderer::drawTexturedCube(position, size, *texture, *texture, *texture);
}

void Model::Draw(Minecraft::Shader& shader, Minecraft::Camera& camera)
{
	
}